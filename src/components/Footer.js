import React from "react";

export default function Footer() {
    return (
        <footer>
            <button id="botao"> Selecione os 3 itens<br/> para fechar o pedido </button>
        </footer>
    );
}